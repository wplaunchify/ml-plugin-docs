name: Scrape Wp File Manager Documentation

on:
  schedule:
    - cron: '40 6 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Wp File Manager scraper
        run: bash scripts/run-scrape.sh --slug=wp-file-manager --mode=sitemap --url=https://filemanagerpro.io/article/ --selector=.article_container --name="WP File Manager"

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet plugins/ INDEX.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.changed == 'true'
        env:
          SCRAPE_SLUG: wp-file-manager
          SCRAPE_NAME: "Wp File Manager"
        run: bash scripts/gh-push-scrape.sh
