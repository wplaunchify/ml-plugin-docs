name: Scrape Affiliatewp Affiliate Landing Pages Documentation

on:
  schedule:
    - cron: '0 11 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Affiliatewp Affiliate Landing Pages scraper
        run: node scraper.js --slug=affiliatewp-affiliate-landing-pages --mode=wp-api --url=https://affiliatewp.com/docs/ --post-type=documentation --name="AffiliateWP - Affiliate Landing Pages"

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet plugins/ INDEX.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.changed == 'true'
        env:
          SCRAPE_SLUG: affiliatewp-affiliate-landing-pages
          SCRAPE_NAME: "Affiliatewp Affiliate Landing Pages"
        run: bash scripts/gh-push-scrape.sh
