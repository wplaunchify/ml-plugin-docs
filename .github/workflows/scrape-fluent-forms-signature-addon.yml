name: Scrape Fluent Forms Signature Addon Documentation

on:
  schedule:
    - cron: '0 12 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Fluent Forms Signature Addon scraper
        run: node scraper.js --slug=fluent-forms-signature-addon --mode=wp-api --url=https://fluentforms.com/docs/ --post-type=docs --taxonomy=doc_category --name="Fluent Forms Signature Addon"

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet plugins/ INDEX.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.changed == 'true'
        env:
          SCRAPE_SLUG: fluent-forms-signature-addon
          SCRAPE_NAME: "Fluent Forms Signature Addon"
        run: bash scripts/gh-push-scrape.sh
