name: Scrape Advanced Custom Fields Pro Documentation

on:
  schedule:
    - cron: '0 14 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Advanced Custom Fields Pro scraper
        run: node scraper.js --slug=advanced-custom-fields-pro --mode=html-scrape --url=https://www.advancedcustomfields.com/resources/ --name="Advanced Custom Fields PRO"

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet plugins/ INDEX.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.changed == 'true'
        env:
          SCRAPE_SLUG: advanced-custom-fields-pro
          SCRAPE_NAME: "Advanced Custom Fields Pro"
        run: bash scripts/gh-push-scrape.sh
