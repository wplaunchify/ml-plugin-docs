name: Scrape Fooevents Multi Day Documentation

on:
  schedule:
    - cron: '10 16 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Fooevents Multi Day scraper
        run: bash scripts/run-scrape.sh --slug=fooevents-multi-day --mode=sitemap --url=https://help.fooevents.com/docs/ --sitemap-index=https://help.fooevents.com/sitemap_index.xml --selector=.foodocs-content --name="FooEvents Multi-Day"

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet plugins/ INDEX.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.changed == 'true'
        env:
          SCRAPE_SLUG: fooevents-multi-day
          SCRAPE_NAME: "Fooevents Multi Day"
        run: bash scripts/gh-push-scrape.sh
