name: Scrape Kadence Blocks Page Builder Toolkit For Gutenberg Editor Documentation

on:
  schedule:
    - cron: '0 18 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Kadence Blocks Page Builder Toolkit For Gutenberg Editor scraper
        run: node scraper.js --slug=kadence-blocks-page-builder-toolkit-for-gutenberg-editor --mode=wp-api --url=https://www.kadencewp.com/help-center/ --post-type=kb_icon --name="Kadence Blocks — Page Builder Toolkit for Gutenberg Editor"

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet plugins/ INDEX.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.changed == 'true'
        env:
          SCRAPE_SLUG: kadence-blocks-page-builder-toolkit-for-gutenberg-editor
          SCRAPE_NAME: "Kadence Blocks Page Builder Toolkit For Gutenberg Editor"
        run: bash scripts/gh-push-scrape.sh
