name: Scrape Fooevents Bookings Documentation

on:
  schedule:
    - cron: '0 1 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Fooevents Bookings scraper
        run: bash scripts/run-scrape.sh --slug=fooevents-bookings --mode=sitemap --url=https://help.fooevents.com/docs/ --sitemap-index=https://help.fooevents.com/sitemap_index.xml --selector=.foodocs-content --name="FooEvents Bookings"

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet plugins/ INDEX.json && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push results
        if: steps.changes.outputs.changed == 'true'
        env:
          SCRAPE_SLUG: fooevents-bookings
          SCRAPE_NAME: "Fooevents Bookings"
        run: bash scripts/gh-push-scrape.sh
